# 🗺️ Diagrama de Arquitetura - ProUni Simulador

## Visão Geral da Arquitetura

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (React)                        │
│                    http://localhost:5173                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ HTTP Requests
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    API Layer (src/lib/api.js)                   │
│                  Base URL: http://localhost:8000                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ authService  │    │cursoService  │    │simulacaoSvc  │
│              │    │              │    │              │
│ • login      │    │ • listar     │    │ • preencher  │
│ • cadastrar  │    │ • getCurso   │    │ • resultado  │
│ • getCand.   │    │ • cadastrar  │    │ • lote       │
│ • atualizar  │    │              │    │ • listarCand │
└──────────────┘    └──────────────┘    └──────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    BACKEND (FastAPI)                            │
│                  http://localhost:8000                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
                    ▼                   ▼
          ┌──────────────┐    ┌──────────────┐
          │ Auth Router  │    │Candidato Rtr │
          │              │    │              │
          │ /login       │    │ /cadastro    │
          │              │    │ /candidatos/ │
          │              │    │ /formulario/ │
          │              │    │ /resultados/ │
          │              │    │ /cursos/     │
          │              │    │ /dados/lote/ │
          └──────────────┘    └──────────────┘
                    │                   │
                    └─────────┬─────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   Database       │
                    │   (SQLAlchemy)   │
                    │                  │
                    │ • Candidato      │
                    │ • Nota           │
                    │ • Curso          │
                    │ • Instituicao    │
                    │ • Inscricao      │
                    └──────────────────┘
```

## Fluxo de Dados - Simulação Completa

```
┌──────────┐
│ Usuário  │
└────┬─────┘
     │
     │ 1. Acessa /Cadastro
     ▼
┌──────────────────────┐
│  Página Cadastro     │
│                      │
│  authService         │──────► POST /cadastro
│  .cadastrar()        │◄────── { ID, nome, email }
└──────────────────────┘
     │
     │ 2. Acessa /Login
     ▼
┌──────────────────────┐
│  Página Login        │
│                      │
│  authService         │──────► POST /login
│  .login()            │◄────── { candidato_id }
└──────────────────────┘
     │
     │ 3. Armazena candidato_id
     │    no AuthContext
     ▼
┌──────────────────────┐
│  Página Dashboard    │
│                      │
│  authService         │──────► GET /candidatos/{id}
│  .getCandidato()     │◄────── { nome, email, ... }
└──────────────────────┘
     │
     │ 4. Clica em "Nova Simulação"
     ▼
┌──────────────────────┐
│  Formulário          │
│  Simulação           │
│                      │
│  cursoService        │──────► GET /cursos/
│  .listarCursos()     │◄────── [ {curso1}, {curso2}, ... ]
└──────────────────────┘
     │
     │ 5. Preenche notas e seleciona curso
     ▼
┌──────────────────────┐
│  simulacaoService    │
│  .preencher          │──────► POST /formulario/{candidato_id}
│  Formulario()        │        { nota, instituicao, curso }
│                      │◄────── { message, candidato_id }
└──────────────────────┘
     │
     │ 6. Exibe animação de processamento (~3s)
     ▼
┌──────────────────────┐
│  Processamento       │
│  (animação)          │
│                      │
│  setTimeout 3000ms   │
└──────────────────────┘
     │
     │ 7. Busca resultado
     ▼
┌──────────────────────┐
│  simulacaoService    │
│  .getResultado()     │──────► GET /resultados/{candidato_id}
│                      │◄────── { aprovado, nota_candidato,
│                      │          nota_minima_corte, curso,
│                      │          diferenca, mensagem }
└──────────────────────┘
     │
     │ 8. Exibe resultado
     ▼
┌──────────────────────┐
│  Página Resultado    │
│                      │
│  ✅ Aprovado! ou     │
│  ❌ Não aprovado     │
│                      │
│  Sua nota: 756.5     │
│  Corte: 650.0        │
│  Diferença: +106.5   │
└──────────────────────┘
```

## Estrutura de Pastas - Frontend

```
src/
│
├── components/          # Componentes reutilizáveis
│   ├── auth/           # Componentes de autenticação
│   │   ├── AuthForm/
│   │   ├── ProtectedRoute/
│   │   └── SocialLoginButton/
│   │
│   ├── simulacao/      # Componentes da simulação
│   │   ├── FormularioSimulacao/
│   │   ├── ProcessamentoSimulacao/
│   │   └── ResultadoSimulacao/
│   │
│   └── ui/             # Componentes UI (shadcn)
│       ├── button.jsx
│       ├── card.jsx
│       ├── input.jsx
│       └── ...
│
├── contexts/           # React Contexts
│   └── AuthContext.jsx # Gerencia autenticação
│
├── entities/           # Lógica de domínio
│   └── Simulacao.jsx   # Persistência Supabase/localStorage
│
├── hooks/              # Hooks customizados
│   └── useSimulacao.js # Hooks para simulação e cursos
│
├── lib/                # Bibliotecas e configurações
│   ├── api.js          # Cliente HTTP
│   ├── supabase.js     # Config Supabase
│   └── utils.jsx       # Utilitários
│
├── pages/              # Páginas da aplicação
│   ├── Cadastro/
│   ├── Dashboard/
│   ├── Login/
│   └── Simulacao/
│
├── services/           # 🆕 Serviços API
│   ├── index.js        # 🆕 Barrel export
│   ├── authService.js  # 🆕 Autenticação
│   ├── cursoService.js # 🆕 Cursos
│   └── simulacaoService.js # 🆕 Simulação
│
├── utils/              # Utilitários
│   └── index.jsx       # Helpers (createPageUrl)
│
├── App.jsx             # Rotas da aplicação
├── layout.jsx          # Layout principal
├── main.jsx            # Entry point
└── index.css           # Estilos globais
```

## Estrutura de Pastas - Backend

```
Backend/
│
├── main.py             # Entry point FastAPI
│
└── db/
    ├── __init__.py
    ├── crud.py         # Operações CRUD
    ├── database.py     # Conexão SQLAlchemy
    ├── models.py       # Modelos ORM
    ├── schemas.py      # Schemas Pydantic
    │
    ├── auth/           # Módulo de autenticação
    │   ├── __init__.py
    │   ├── auth.py     # Lógica de auth
    │   └── router.py   # Rotas de auth
    │
    └── routers/        # Routers adicionais
        └── candidato_router.py  # Rotas principais
```

## Endpoints API Mapeados

```
╔══════════════════════════════════════════════════════════════╗
║                    AUTENTICAÇÃO                              ║
╠══════════════════════════════════════════════════════════════╣
║ POST   /cadastro              → Criar candidato              ║
║ POST   /login                 → Autenticar candidato         ║
╚══════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════╗
║                    CANDIDATOS                                ║
╠══════════════════════════════════════════════════════════════╣
║ GET    /candidatos/           → Listar todos (admin)         ║
║ GET    /candidatos/{id}       → Buscar por ID                ║
║ PUT    /candidatos/{id}       → Atualizar dados              ║
╚══════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════╗
║                    CURSOS                                    ║
╠══════════════════════════════════════════════════════════════╣
║ GET    /cursos/               → Listar cursos                ║
║ GET    /cursos/{id}           → Buscar curso                 ║
║ POST   /cursos/               → Cadastrar curso              ║
╚══════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════╗
║                    SIMULAÇÃO                                 ║
╠══════════════════════════════════════════════════════════════╣
║ POST   /formulario/{cand_id}  → Preencher formulário         ║
║ GET    /resultados/{cand_id}  → Obter resultado              ║
║ POST   /dados/lote/           → Inserir dados teste (lote)   ║
╚══════════════════════════════════════════════════════════════╝
```

## Configuração de Ambiente

```
┌─────────────────────────────────────────────────────────┐
│                  .env (Frontend)                        │
├─────────────────────────────────────────────────────────┤
│ VITE_USE_REAL_API=true                                  │
│ VITE_API_URL=http://localhost:8000                      │
│                                                         │
│ # Opcional (Supabase)                                   │
│ VITE_SUPABASE_URL=...                                   │
│ VITE_SUPABASE_ANON_KEY=...                              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                  Backend Config                         │
├─────────────────────────────────────────────────────────┤
│ • CORS configurado para aceitar frontend                │
│ • SQLAlchemy database URL                               │
│ • Porta: 8000                                           │
└─────────────────────────────────────────────────────────┘
```

## Importações Principais

```javascript
// ✅ CORRETO - Usar barrel imports
import { authService, cursoService, simulacaoService } from '@/services'

// ✅ CORRETO - Importação específica
import { authService } from '@/services/authService'

// ✅ CORRETO - Usar hooks
import { useSimulacao, useCursos } from '@/hooks/useSimulacao'

// ✅ CORRETO - Usar context
import { useAuth } from '@/contexts/AuthContext'

// ✅ CORRETO - Usar utils
import { createPageUrl } from '@/utils'
```

## Comandos de Desenvolvimento

```bash
# Backend
./start-api.ps1              # Windows PowerShell
python -m uvicorn main:app --reload  # Manual

# Frontend
npm run dev                  # Desenvolvimento
npm run build                # Build produção
npm run preview              # Preview build
```

---

**Este diagrama serve como referência rápida da arquitetura completa do projeto.**
